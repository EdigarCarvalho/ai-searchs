export function visualizePath(path: string[], totalDistance: number = 0): void {
    console.log('Path:', path.join(' -> '));
    console.log('Total distance:', totalDistance);
    
    // In a real application, this would update the UI
}